# Local build/push credentials for the Scaleway Container Registry.
# Copy this file to `.env` (which is git-ignored) and fill in real values.
# Create the keys in: Scaleway console → IAM → API keys.
# These are used ONLY by deploy.sh on your machine — never by the running service.

SCW_ACCESS_KEY=
SCW_SECRET_KEY=
